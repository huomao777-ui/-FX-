# 联系人场景开发约束

1. 后续优先修改脚本，不直接改 `res://界面/场景/联系人/联系人.tscn`。
2. 只有在“挂载新脚本”或“修复场景已损坏的资源引用”这类无法绕开的情况下，才允许最小范围修改场景文件。
3. 除必要挂载外，不修改父节点和手机相关节点，只处理 `APP` 及其下方节点。
4. 新逻辑优先放在 `res://沙盒/测试/` 下验证，确认稳定后再考虑迁回联系人目录。
5. 如果后续要调整交互、碰撞、缩放、连接线或视差，优先通过节点参数和脚本导出参数完成，不通过反复改场景文本硬写。
